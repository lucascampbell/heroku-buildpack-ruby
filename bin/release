#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "language_pack"
puts "in side of release"
if pack = LanguagePack.detect(ARGV[0], ARGV[1])
  puts "running release"
  system "cp -rv /app/rhoconnect-resource/* /app/"
  puts pack.release
end

